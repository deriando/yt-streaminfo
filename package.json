{
  "name": "yt-streaminfo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app/*"
  ],
  "scripts": {
    "dev-webui": "cd ./app/webui/src && npx serve",
    "dev-worker": "cd ./app/worker && npx @cloudflare/wrangler dev --host='localhost'",
    "deploy-worker": "cd ./app/worker && npx @cloudflare/wrangler publish --env production"
  }
}
