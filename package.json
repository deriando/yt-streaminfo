{
  "name": "yt-streaminfo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app/*"
  ],
  "scripts": {
    "dev-webui": "cd ./app/webui/src && yarn dlx serve",
    "dev-worker": "cd ./app/worker && yarn dlx @cloudflare/wrangler dev --host='localhost'",
    "deploy-worker": "cd ./app/worker && yarn dlx @cloudflare/wrangler publish --env production"
  }
}
